version: v1.0
name: 8CPU time test #2 
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      jobs:
      - name: Test
        commands:
          - checkout
          - make test
          - echo done

